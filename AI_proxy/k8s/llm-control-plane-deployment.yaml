apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-control-plane
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: llm-control-plane
  template:
    metadata:
      labels:
        app: llm-control-plane
    spec:
      nodeSelector:
        kubernetes.io/hostname: tsl01.tech-sphere.pro
      containers:
        - name: api
          image: llm-control-plane:fix25
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
              name: http
          envFrom:
            - secretRef:
                name: llm-control-plane-secrets
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: llm-control-plane
  namespace: default
spec:
  selector:
    app: llm-control-plane
  ports:
    - name: http
      port: 8000
      targetPort: http
  type: LoadBalancer
